# V2.4 consolidated accelerated candidate

Branch: `v2.4-accelerated-integration`

Rollback branch: `v2.4-development` at `ceda10d`

Production branch: `main` (V2.3.3 remains production)

This branch combines the cumulative V2.4 work that had previously been organized as internal development milestones:

- CPU-aware accelerated execution with serial-equivalence checks.
- Canonical fail-closed seven-model registry.
- Matched chronological V2.3.3-versus-V2.4 validation and promotion gates.
- Hash-chained point-in-time prospective evidence ledger.
- Closing-line, outcome, schedule-integrity, leakage, and provenance audit support.
- Immutable official-`gamePk` starting-pitcher identity and statistics snapshots.
- Starter-change auditing and future-training export support.

The internal milestone commits are retained for auditability, but users import only this one cumulative branch and bundle.

## Promotion state

V2.4 remains `PENDING`. Retrospective probability-quality gates pass, but promotion is blocked until the prospective sample, CLV, schedule-integrity, target-leakage, point-in-time provenance, and final locked-holdout requirements are satisfied.

Do not merge this branch into `main` or unlock the final holdout yet.
